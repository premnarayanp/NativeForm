import React, { useEffect, useState } from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';
import { useNavigation } from '@react-navigation/native';

const FormViewer = () => {
    useEffect(() => {
    }, []);

    return (
        <View>
            <Text>Form Viewer</Text>
        </View>
    );
};
export default FormViewer;

